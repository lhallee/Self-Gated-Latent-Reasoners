# Historical interactive viewer

The Streamlit application replayed individual routes and displayed gate probabilities and probe reconstructions from the previous flat-vector model. Its checkpoint schema and imports are not maintained against the current `(b, l, d)` core.

Streamlit is intentionally excluded from the core dependencies. This viewer is historical context rather than a supported analysis command.

