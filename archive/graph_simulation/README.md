# Historical graph simulation

The graph utilities generated standalone propagation simulations and animations. They are not imported by the SGLR package and are not part of model training, evaluation, or figure generation.

NetworkX and animation-specific dependencies are intentionally excluded from the core environment.

